[tool.poetry]
name = "helpers"
version = "0"
description = ""
authors = []
packages = [
    { include = "app" },
    { include = "helpers.py" },
    { include = "mddoctest.py"},
    { include = "pkg" },
]

[build-system]
requires = ["poetry"]
build-backend = "poetry.masonry.api"
