from setuptools import setup

setup(name='helpers', py_modules=['helpers'])

setup(name='django_settings_module', py_modules=['django_settings_module'])

# tests/contrib/test_django.py::test_dispatch_request
setup(name='urlconf_dispatch_request', py_modules=['urlconf_dispatch_request'])
